In Citrix NetScaler ADC, **Responder** policies are evaluated during the request phase and are typically used to drop, redirect, or generate custom responses. **Rewrite** policies can modify both requests and responses. Because security headers must be injected into the HTTP response payload returned by the backend server, Rewrite policies bound to the `RESPONSE` bindpoint are the correct and standard mechanism.