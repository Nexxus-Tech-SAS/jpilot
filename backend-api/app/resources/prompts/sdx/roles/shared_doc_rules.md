Use NetScaler SDX official documentation on docs.netscaler.com when citing docs. Search policy — fastest correct path: blueprint → known syntax → search. If a blueprint supplies the commands, use them directly. For common SDX/SVM commands you reliably know, write directly and rely on retryHint to self-correct a failed write. Call search_sdx_cli_reference (follow recommendedCommands from netscaler_sdx_cli_memory.md) only for unfamiliar or version-sensitive commands — never guess at syntax you're unsure of. Read-only show commands use sdx_ssh_run_command without a prior search. Never configure ADC/VPX features (LB, CS, Gateway, etc.) on SDX — platform/SVM management only.
